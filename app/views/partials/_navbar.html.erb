<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
    <a class="navbar-brand" href="#">Voted News App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav  align-items-end ms-auto mb-2 mb-lg-0">
    <% if user_signed_in? %>
        <li class="nav-item">
            <%= link_to @sort==1 ? 'Sort by Votes': 'Sort by Retrieval', 
                { sort: @sort },
                class: 'btn btn-link btn-small nav-link btn-inline' %>            
        </li>
        <li class="nav-item">
            <%= button_to 'Refresh', 
                refeed_item_path(0), method: :put,
                class: 'btn btn-link btn-small nav-link btn-inline' %>            
        </li>
        <span class="navbar-text"><em><small><%= [current_user.first_name, current_user.last_name].join(' ') %></small></em></span> 
        <li class="nav-item">
            <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn btn-link nav-link btn-inline' %>
        </li>
    <% else %>
        <li class="nav-item">
            <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
            <%= link_to 'Register', new_user_registration_path, class: 'nav-link' %>
        </li>
    <% end %>
    </ul>
    </div>
    </div>
</nav>
