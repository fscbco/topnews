<% @top_stories.each do |id| %>
  <% story = details(id: id) %>
  <h3><%= story.raw_data.dig("title") %></h3>
  <h4>By <%= story.raw_data.dig("by") %></h4>
  <h4>URL <%= story.raw_data.dig("url") %></h4>
  <%= "----------------------------" %>
<% end %>
